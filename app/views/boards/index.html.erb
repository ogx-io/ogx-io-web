<div class="row">
  <ol class="breadcrumb pull-left">
    <li>boards</li>
  </ol>
  <%= link_to '申请新版', new_board_application_path, class: 'breadcrumb pull-right' %>
</div>
<div class="row">
  <div class="col-sm-4 col-md-3 lead">
    <% @boards.each do |board| %>
      <%= link_to board.name, board_topics_path(board) %>
    <% end %>
  </div>
</div>
<br>

