<div class="col-sm-10 right-side-layout">
  <div class="row">
    <ol class="breadcrumb pull-left">
      <li><%= link_to 'boards', boards_path %></li>
      <li><%= link_to @board.path, show_board_path(@board.path) %></li>
      <li>posts</li>
    </ol>
  </div>

  <div class="board-main">
    <%= render 'boards/board_head' %>
    <%= render 'boards/board_intro' %>
    <%= render partial: 'boards/board_nav', locals: {index: 2} %>
    <div class="board-content">
      <%= render partial: "posts/post_list_item", as: :post, collection: @posts %>
    </div>
    <div class="board-pagination">
      <%= paginate @posts, left: 1, right: 1, :theme => 'bootstrap3' %>
    </div>
  </div>
  <%= render partial: 'boards/board_right_side', locals: {board: @board} %>
</div>

