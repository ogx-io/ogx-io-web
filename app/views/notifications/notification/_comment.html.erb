<td class="avatar"><%= avatar notification.comment.user, 40 %></td>
<td class="content">
  <div class="title">
    <%= user_link notification.comment.user %>
    评论了你的帖子
    <%= link_to notification.comment.commentable.title, show_post_path(notification.comment.commentable.sid) + "#comment-#{notification.comment.id}" %>
    <% if !notification.read %>
        <span class="new-item-tip">新提醒</span>
    <% end %>
  </div>
  <div class="details"><%= notification.comment.body %></div>
</td>