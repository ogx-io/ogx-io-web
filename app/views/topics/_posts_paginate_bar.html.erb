<nav class="topic-bar clearfix">
  <%= t('global.count.post') %>: <%= @all_posts.count %>
  <%= paginate @posts, left: 1, right: 1 %>
  <div class="right-links pull-right">
    <% if policy(@topic).update? %>
        <% if @topic.deleted? %>
            <%= link_to t('views.action.resume'), resume_topic_path(@topic), method: :patch %>
        <% else %>
            <%= link_to t('views.action.delete'), topic_path(@topic), method: :delete %>
        <% end %>
    <% end %>
    <% if policy(@topic).toggle_lock? %>
        <% if @topic.locked? %>
            <%= link_to t('views.action.unlock'), toggle_lock_topic_path(@topic), method: :patch %>
        <% else %>
            <%= link_to t('views.action.lock'), toggle_lock_topic_path(@topic), method: :patch %>
        <% end %>
    <% end %>
    <%= link_to t('views.action.new_topic'), new_board_post_path(@board) %>
    <%= link_to t('views.action.reply'), new_board_post_path(@board, parent_id: @first.id) %>
  </div>
</nav>