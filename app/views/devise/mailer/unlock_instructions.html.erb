<%= t('views.users.mailer.unlock_instructions',
      user: @resource.name,
      unlock_url: unlock_url(@resource, unlock_token: @token),
      unlock_url_link: link_to('解锁账号', unlock_url(@resource, unlock_token: @token))
    ).html_safe %>
