<%= t('users.mailer.confirmation_instructions_html',
      user: @resource.name,
      confirmation_url: confirmation_url(@resource, confirmation_token: @token),
      confirmation_url_link: link_to('确认我的账号', confirmation_url(@resource, confirmation_token: @token))
    ) %>
