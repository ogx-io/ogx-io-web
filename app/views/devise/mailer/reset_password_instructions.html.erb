<%= t('views.users.mailer.reset_password_instructions',
      user: @resource.name,
      edit_password_url: edit_password_url(@resource, reset_password_token: @token),
      edit_password_url_link: link_to('更换密码', edit_password_url(@resource, reset_password_token: @token))
    ).html_safe %>
