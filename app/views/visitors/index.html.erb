<div class="col-sm-7 front-page right-side-layout">
  <div class="row">
    <ol class="breadcrumb pull-left">
      <li>最新动态</li>
    </ol>
  </div>

  <% @topics.each do |topic| %>
    <%= render partial: "topics/topic_list_item", locals: {topic: topic} %>
  <% end %>
</div>
