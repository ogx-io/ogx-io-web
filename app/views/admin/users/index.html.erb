<%= render partial: 'admin/common/admin_nav', locals: {index: 5} %>
<%= render 'users_paginate_bar' %>
<table class="table table-hover">
  <thead>
  <th>ID</th>
  <th><%= t('views.users.avatar') %></th>
  <th><%= t('views.users.user_name') %></th>
  <th><%= t('views.users.nick') %></th>
  <th><%= t('views.users.email') %></th>
  <th><%= t('views.admin.users.sign_in_times') %></th>
  <th><%= t('views.admin.users.last_signed_in_at') %></th>
  <th><%= t('views.admin.users.last_visited_at') %>最后访问</th>
  <th><%= t('views.admin.users.role') %></th>
  <th><%= t('views.admin.users.operator') %></th>
  </thead>
  <tbody>
  <% @users.each do |user| %>
  <%= render user %>
  <% end %>
  </tbody>
</table>
<%= render 'users_paginate_bar' %>

