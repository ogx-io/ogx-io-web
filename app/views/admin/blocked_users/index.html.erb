<%= render partial: 'admin/common/admin_nav', locals: {index: 3} %>
<div class="blocked-users-mng">
  <%= render 'blocked_users_filter_and_adder' %>
  <%= render 'blocked_users_paginate_bar' %>
  <table class="table table-hover">
    <thead>
    <th><%= t('admin.blocked_users.blocked_user') %></th>
    <th><%= t('global.board') %></th>
    <th><%= t('admin.blocked_users.operator') %></th>
    <th><%= t('admin.blocked_users.blocked_at') %></th>
    <th><%= t('global.operate') %></th>
    </thead>
    <tbody>
    <%= render @blocked_users %>
    </tbody>
  </table>
  <%= render 'blocked_users_paginate_bar' %>
</div>