<%= render partial: 'admin/common/admin_nav', locals: {index: 2} %>
<%= render 'admin/common/post_comment_filter' %>
<div class="posts-mng">
  <%= render 'posts_paginate_bar' %>
  <table class="table table-hover">
    <thead>
    <th>ID</th>
    <th><%= t('global.title') %></th>
    <th><%= t('global.board') %></th>
    <th><%= t('admin.posts.parent') %></th>
    <th><%= t('global.author') %></th>
    <th><%= t('admin.posts.posted_at') %></th>
    <th>IP</th>
    <th><%= t('global.status') %></th>
    <th><%= t('global.operate') %>操作</th>
    </thead>
    <tbody>
    <%= render @posts %>
    </tbody>
  </table>
  <%= render 'posts_paginate_bar' %>
</div>